[project]
name = "levipy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sympy>=1.14.0",
]

[tool.setuptools.packages.find]
include = ["levipy*"]
